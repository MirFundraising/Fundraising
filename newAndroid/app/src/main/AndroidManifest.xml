<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mirand">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MirAnd">
        <activity android:name=".card_row"></activity>
        <activity android:name=".ui.insides.InsideFundraisingActivity" />
        <activity android:name=".ui.adds.create_new_fundraising" />
        <activity android:name=".ui.insides.inside_cluster_activity" />
        <activity android:name=".ui.adds.add_new_cluster" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.intro.activity_register" />
        <activity android:name=".ui.intro.activity_login" />
        <activity
            android:name=".ui.intro.activity_welcome"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>